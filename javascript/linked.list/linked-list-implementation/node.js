'use strict';

class Node {
    constructor(value, next = null) {
      (this.value = value), (this.next = next);
    }
  }


  
  class LLNode{
    constructor(v, n = null){
      (this.value = next), (this.next = next);
    }
  }


  module.exports =
  Node,
  LLNode;